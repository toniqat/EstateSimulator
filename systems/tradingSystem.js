/**
 * Trading System
 * Handles trading post operations (deprecated - replaced by TradingPostSystem with order-based mechanics)
 */
class TradingSystem {
    constructor(gameState, stashManager) {
        this.gameState = gameState;
        this.stashManager = stashManager;
    }
}
